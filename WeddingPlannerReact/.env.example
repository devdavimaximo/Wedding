# Copie para .env.local (não commitado). Na Vercel, defina a mesma variável em Settings → Environment Variables.
#
# URL pública da API ASP.NET (sem barra no final). Em npm run dev, deixe vazio para usar o proxy do Vite → localhost.
# Exemplo após hospedar a API: https://wedding-planner-api.azurewebsites.net
VITE_API_BASE_URL=
